---
// Componente simple para mostrar una tarjeta de proyecto en la Landing Page
import type { CollectionEntry } from 'astro:content';

export interface Props {
  project: CollectionEntry<'proyectos'>;
}

const { project } = Astro.props;
const { title, summary, gallery, image } = project.data;

// Obtener la primera imagen de la galería si existe
const firstImage = image ?? (gallery?.length ? gallery[0].image_path : '/placeholder.png');
---

<a href={`/proyectos/${project.slug}`} class="block shadow-xl hover:shadow-2xl transition duration-300 overflow-hidden bg-primary text-text-inverted">
  <div class="h-40 bg-cover bg-center" style={`background-image: url(${firstImage})`}>
    <!-- Placeholder para la imagen, en un proyecto real usarías <Image /> de Astro -->
  </div>
  <div class="p-5">
    <h3 class="text-xl font-semibold mb-2 text-accent">{title}</h3>
    <p class="text-sm opacity-80 mb-3">{summary}</p>
    <span class="text-xs font-medium bg-accent py-1 px-3 inline-block text-text-inverted">Ver Caso de Éxito →</span>
  </div>
</a>
